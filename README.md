This repository was created for design patterns training. You can clone it and continue your training with yourself code.

Install for all dependencies;

``composer install``

Run for tests;

``./vendor/bin/phpunit src/tests --colors``